# AutoEdit — зависимости Python
#
# FFmpeg — отдельная системная программа, не пакет pip; команда
# `autoedit check` проверяет, что он установлен, и подсказывает, как
# поставить его на вашей ОС.
#
# При первом запуске autoedit check/render модель faster-whisper
# скачивается один раз из интернета (Hugging Face) и дальше работает
# офлайн.
faster-whisper>=1.0
Pillow>=10.0

# Необязательно: экспорт таймлайна для DaVinci Resolve (этап 6).
# На некоторых системах (например, очень новый Python, для которого
# ещё нет готовых пакетов) сборка из исходников требует установленный
# компилятор C++ и может не заработать — тогда просто не ставьте эти
# два пакета: check/render продолжат работать, только без
# timeline.otio/timeline.fcpxml. Ставится отдельно: pip install .[export]
# opentimelineio>=0.16
# otio-fcpx-xml-adapter>=1.0
