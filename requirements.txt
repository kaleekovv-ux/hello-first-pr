# AutoEdit — зависимости Python
#
# Этап 1 (текущая версия) использует только стандартную библиотеку —
# устанавливать через pip пока нечего. FFmpeg — отдельная системная
# программа, не пакет pip; команда `autoedit check` проверяет, что он
# установлен, и подсказывает, как поставить его на вашей ОС.
#
# На следующих этапах здесь появятся:
#   faster-whisper   — распознавание речи с таймкодами слов (этап 2)
#   opentimelineio   — экспорт таймлайна для DaVinci Resolve (этап 6)
